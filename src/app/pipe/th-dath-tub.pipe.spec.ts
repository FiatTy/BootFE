import { ThDathTubPipe } from './th-dath-tub.pipe';

describe('ThDathTubPipe', () => {
  it('create an instance', () => {
    const pipe = new ThDathTubPipe();
    expect(pipe).toBeTruthy();
  });
});
